# Study Plan — moved to the README

The full study plan (schedule, role→study mapping, day-by-day detail, progress
tracker) now lives at the top level in **[`../README.md`](../README.md)** so it's
visible on the GitHub landing page.

The per-day study notes remain here in `docs/` as `dayNN_*.md`.
